# Compatibility helpers needed by selected Oh My Zsh plugins.
# Must be first so $ZSH / $ZSH_CACHE_DIR are prepared for OMZ plugins.
getantidote/use-omz

# Ensure the full Oh My Zsh repo is cloned for use-omz / $ZSH.
ohmyzsh/ohmyzsh kind:clone

# Oh My Zsh plugins.
ohmyzsh/ohmyzsh path:plugins/sudo
ohmyzsh/ohmyzsh path:plugins/docker
ohmyzsh/ohmyzsh path:plugins/docker-compose
ohmyzsh/ohmyzsh path:plugins/extract
ohmyzsh/ohmyzsh path:plugins/history

# Extra completion definitions (fpath only; cloned for use before/with compinit).
zsh-users/zsh-completions path:src kind:fpath

# Fuzzy regular Tab completion (after compinit; before widget wrappers).
Aloxaf/fzf-tab

# Interactive git via fzf.
wfxr/forgit

# Interactive enhancements.
zsh-users/zsh-history-substring-search
zsh-users/zsh-autosuggestions

# Keep highlighting last because it wraps the ZLE widgets.
zdharma-continuum/fast-syntax-highlighting
